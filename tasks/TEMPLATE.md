  ## Task: [name]

  ### Problem (2 sentences max)
  [What is wrong or missing. Include the symptom and the suspected cause.]

  ### Relevant files
  - `src/X.py` lines ~L1-L2: [what's there]
  - `configs/Y.yaml`: [what matters]

  ### What NOT to touch
  [Files/behaviors that must not break]

  ### Acceptance criterion
  [Command to run and what it should output: "bash run_physics_validation.sh → Phase 1 PASS"]

  ### What's already been tried (don't repeat)
  - Approach A: why it failed
  - Approach B: why it failed

  ---
  Memory Protocol (what to save vs. re-derive)
  
  Save to memory (survives between sessions):
  - Architectural decisions and WHY
  - What was tried and confirmed broken
  - Acceptance criteria for each milestone
  - The 5-day plan with daily targets

  Re-derive from code (never memorize):
  - File paths and line numbers
  - Function signatures 
  - YAML parameter values

